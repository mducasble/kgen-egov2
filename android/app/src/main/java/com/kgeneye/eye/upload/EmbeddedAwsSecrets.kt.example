package com.kgeneye.eye.upload

/**
 * Local (gitignored) AWS IAM credentials.
 * Copy this file to `EmbeddedAwsSecrets.kt` and fill in the values.
 * Never commit the real secrets file.
 */
object EmbeddedAwsSecrets {
    const val accessKeyId = "AKIA_REPLACE_ME"
    const val secretAccessKey = "REPLACE_ME"
}
